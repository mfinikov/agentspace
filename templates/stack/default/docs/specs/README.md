# specs

One file per user-visible outcome: `<slug>.md`. A spec is done when its
acceptance criteria are observable checks — something a script or a person can
evaluate true or false. Adjectives ("fast", "robust") are not criteria.

Use the `write-spec` skill. Do not start building before the criteria exist:
they are the only thing that makes verification possible.
