# plans

One file per spec: `<slug>.md`. Ordered steps, each independently verifiable,
each leaving `scripts/check.sh` green.

Keep the checkboxes true **as you work**, not at the end. This file is the
handoff — if the session dies mid-task, it is all the next agent has.
