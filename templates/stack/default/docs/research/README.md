# research

External facts, one file per question: `<topic>.md`.

Every claim carries a source — a URL, or a file path plus the version you
actually have installed. A finding without a source is an opinion, and a future
agent cannot tell the difference.

Record what you could not establish. Unknowns are findings.
